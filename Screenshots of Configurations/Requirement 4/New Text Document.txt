1. Show HR ACL and Interface Binding
cisco
Copy
Edit
HR# show access-lists ACL-SQL-HR
HR# show run interface FastEthernet0/1
(Shows ACL permitting only HR→SQL, and applied inbound.)

2. Show HR Static Route to SQL LAN
cisco
Copy
Edit
HR# show ip route 10.20.30.0
(Displays the static route via 10.10.11.1.)

3. Positive Connectivity Test (HR → SQL)
cisco
Copy
Edit
HR# ping 10.20.30.10
(Should be !!!!!.)

4. Negative Connectivity Test (EXEC blocked)
On EXEC, first show the LAN interface IP:

cisco
Copy
Edit
EXEC# show ip interface brief | include FastEthernet0/1
Then attempt the ping using the interface as source:

cisco
Copy
Edit
EXEC# ping 10.20.30.10 source FastEthernet0/1
(Should fail: ......)

5. Optional: Show LDS Interface (SQL LAN)
cisco
Copy
Edit
LDS# show ip interface brief | include FastEthernet0/1
(To prove the SQL server network is up. If you used the secondary IP for SQL host: show run interface FastEthernet0/1.)

Take screenshots of each output. Together they prove:

HR has the ACL.

HR reaches SQL server.

EXEC is blocked.

SQL LAN exists.